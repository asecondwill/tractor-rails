<% 
  add_breadcrumb "Pages", :admin_pages_path 
  add_breadcrumb @page.title, :admin_page_path
  add_breadcrumb "Edit", :edit_admin_page_path
%>

<%= render 'form', page: @page %>

<%= render "admin/sections/list", item: @page, description: 'Page Sections' %>